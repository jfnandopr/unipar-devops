services:
  web:
    build: my-grails-app
    restart: always
    depends_on:
      - db
    ports:
      - 8089:8080
    networks:
      - frontend
      - backend
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: 123456

  db:
    image: mysql
    restart: always
    environment:
      MYSQL_DATABASE: my-grails-app
      MYSQL_ROOT_PASSWORD: 123456
    volumes:
      - db:/var/lib/mysql
    networks:
      - backend
      
  adminer:
    image: adminer
    restart: always
    depends_on:
      - db
    ports:
      - 8088:8080
    networks:
      - backend

volumes:
  wordpress:
  db:

networks:
  frontend:
  backend: